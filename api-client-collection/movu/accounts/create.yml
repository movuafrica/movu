info:
  name: create
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3001/accounts
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
